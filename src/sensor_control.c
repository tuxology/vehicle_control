#include <stdio.h>
#include <string.h>
#include "engine.h"
#include "bus.h"

int get_imaging_command() {
    // 1. fuse sensor data and compute commands
    // 2. generate command to send on the bus
    
    return 2; /* When used directly, without verifying the bus, this would cause engine shutdown */
}